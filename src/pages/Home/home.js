import { styled } from "styled-components";

export const CssHomeMain = styled.div`
    display: flex;
    flex-direction: column;
    gap:20px;

    margin-top: 20px;
    p{
        color: var(--grey-1);
    }
`